//@flow

import React, { Component } from "react";
import { View, Text } from "react-native";

/**
 * @interface Props
 */
interface Props {}

const ThoughtView = (props: Props) => (
  <View>
    <Text>Hey</Text>
  </View>
);

export default ThoughtView;
